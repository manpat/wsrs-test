[package]
name = "wsclient"
version = "0.1.0"
authors = ["manpat <pat@patsown.com>"]
build = "../build.rs"

[dependencies]
libc = { version = "0.2", default-features = false }
common = { path = "../common" }
